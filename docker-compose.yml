services:
  web:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "8000:8000"  
    volumes:
      - .:/app  
    environment:
      - PYTHONPATH=/app/src
    command: ["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
